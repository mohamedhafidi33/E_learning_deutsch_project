let btnshow =document.querySelection('button');
let resultat=document.querySelection('h1');
btnshow.addEventListener('click')
btwShow.addEventListener('click', () ==> {
	let checkbox = document.querySelection("input [type ="checkbox"]:checke
		d
} );
